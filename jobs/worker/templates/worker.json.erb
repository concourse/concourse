<%=
  name_prefix = spec.id.split("-")[0]

  payload = {
    "name" => "#{name_prefix}-HOSTNAME_REPLACE_ME",
  }

  if_p("certs_path") do |certs_path|
    payload["certs-path"] = certs_path
  end

  payload.to_json
%>
