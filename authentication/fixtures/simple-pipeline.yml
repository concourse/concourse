---
jobs:
  - name: simple
    plan:
    - task: say-simple
      config:
        platform: linux
        image_resource:
          type: docker-image
          source: {repository: busybox}
        run:
          path: echo
          args: ["simple"]
