#!/bin/bash
# vim: set ft=sh

set -e -x

pushd concourse/src/github.com/concourse/atc/web
  npm install --silent

  npm install phantomjs --silent

  export PATH=$(npm bin):$PATH

  gulp jasmine-cli

  pushd elm
    elm-package install --yes
    elm-test TestRunner.elm
  popd
popd
