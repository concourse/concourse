---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/git-resource

inputs:
- name: concourse

outputs:
- name: message

params:
  FROM_COMMIT_FILE:
  TO_COMMIT_FILE:
  HEADING:

run:
  path: concourse/ci/scripts/issues-for-commit-range
