---
platform: linux

image_resource:
  type: docker-image
  source:
    repository: concourse/atc-ci

inputs:
  - name: concourse
  - name: version
  - name: linux
  - name: darwin
  - name: windows

outputs:
  - name: fly-binaries

params:
  platform: linux

run:
  path: concourse/ci/scripts/tarball-flies
