---
platform: linux

image_resource:
  type: registry-image
  source:
    repository: concourse/atc-ci

inputs:
- name: concourse
- name: fly-rc
- name: endpoint-info

params:
  WEB_IP:
  WEB_USERNAME:
  WEB_PASSWORD:
  PIPELINE_NAME:

run:
  path: concourse/ci/scripts/verify-uber-pipeline
