---
resource_types:
- name: custom-type
  type: mirror
  source:
    mirror_self: true
    initial_version: custom-type-version

resources:
- name: git-repo
  type: custom-type
  source: {}

jobs:
- name: input-test
  plan:
  - get: git-repo
  - task: input-test
    file: git-repo/task.yml
