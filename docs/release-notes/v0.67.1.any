#language anatomy

\title{v0.67.1}

\margin-note{
  Run \reference{fly-sync}{\code{fly sync}} to upgrade Fly after deploying
  v0.67.1!
}

\list{
  Fixed a panic that would crash Concourse if a worker was recreated that had
  volumes on it.
}{
  We now log as we are performing migrations to make it easier to spot any slow
  ones.
}{
  Fly will provide better validations around missing or extra inputs and
  outputs to \code{fly execute}.
}
