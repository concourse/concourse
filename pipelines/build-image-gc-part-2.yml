---
jobs:
- name: some-job
  plan:
  - task: wait
    config:
      platform: linux
      image_resource:
        type: docker-image
        source: {repository: alpine}
      run:
        path: echo
        args: ['Hello, world!']
