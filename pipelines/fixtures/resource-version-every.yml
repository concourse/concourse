---
resources:
  - name: some-git-resource
    type: git
    source:
      uri: {{origin-git-server}}
      branch: master

jobs:
  - name: some-passing-job
    plan:
      - get: some-git-resource
        trigger: true
        version: every
      - task: curl-server
        config:
          platform: linux
          inputs:
            - name: some-git-resource
              path: .
          run:
            path: tail
            args: ["-1", "guids"]
