---
jobs:
  - name: missing-outputs-job
    plan:
      - task: missing-outputs
        config:
          platform: linux
          run:
            path: touch
            args: ["abandonedfile"]
      - task: input-deprived
        config:
          platform: linux
          inputs:
            - name: missing-outputs
              path: .
          run:
            path: /bin/true
