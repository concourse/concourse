---
jobs:
  - name: try-job
    plan:
      - try:
          task: failing-task
          config:
            platform: linux
            run:
              path: exit
              args: ["1"]
      - task: passing-task
        config:
          platform: linux
          run:
            path: echo
            args: ["passing-task succeeded"]
