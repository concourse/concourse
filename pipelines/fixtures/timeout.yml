---
jobs:
  - name: duration-successful-job
    plan:
      - task: passing-task
        timeout: 10s
        config:
          platform: linux
          run:
            path: echo
            args: ["passing-task succeeded"]

  - name: duration-fail-job
    plan:
      - task: timeout-task
        timeout: 5s
        config:
          platform: linux
          run:
            path: sleep
            args: ["10"]
