roles:
  - name: member
    local:
      users: ["some-user"]
  - name: viewer
    allow_all_users: true
  - name: owner
    github:
      users: ["some-github-user"]
    local:
      users: ["some-admin"]
    oauth:
      groups: ["some-oauth-group"]

